import type { Chapter } from "../../../src/types.ts";
import { code, fig, note, table, p, ul, ol, ch } from "../../../src/ui.ts";
import en, { DIAL_SVG } from "../../chapters/c00.ts";

/** 模拟器小节直接复用英文版：交互控件的文案写在 SVG 和脚本里，
 *  单独翻译会让它和 code/ 里的可运行文件对不上。 */
const explore = en.sections.find((s) => s.id === "explore")!;

const chapter: Chapter = {
  ...en,
  title: "智能体到底是什么",
  subtitle: "循环、旋钮，以及三种不算智能体的东西",
  blurb:
    "智能体是一个让模型决定下一步做什么的程序：它在一个循环里运行，手上的工具能真的改变世界。关于智能体的一切昂贵之处，都是从这一句话里长出来的。",

  sections: [
    {
      id: "motivation",
      kicker: "动机",
      title: "三个从外面看一模一样的程序",
      html:
        p(`用户输入 <em>"4471 号订单现在什么状态？"</em>，得到的回复是 <em>"周二已发货，今天正在派送。"</em> 有三种完全不同的程序都能产生这段对话，而从对话记录里你根本看不出面对的是哪一种。账单看得出来。p99 延迟看得出来，数据库挂掉的那天也看得出来。`) +
        ol([
          `<strong>一条流水线。</strong> 用正则把订单号抠出来，查数据库，套模板拼一句话。全程不涉及模型，或者最后调一次模型把话说得顺一点。40&nbsp;毫秒跑完，坏的时候坏得明明白白，而且每次都以同样的方式坏。`,
          `<strong>一条提示链。</strong> 第一次模型调用给意图分类，你的代码按分类分支，第二次模型调用根据查到的那行数据写回复。两次调用，形状固定，大约 900&nbsp;毫秒。整个流程在跑之前就能画成流程图。`,
          `<strong>一个智能体。</strong> 你给模型一个目标和四个工具 —— <code>lookup_order</code>、<code>get_tracking</code>、<code>search_policy</code>、<code>email_customer</code> —— 然后让它自己决定调哪个、按什么顺序调、调几次、什么时候算做完。通常两次调用。有时候九次。有一次，令人难忘地，四十一次。`,
        ]) +
        p(`三种都能回答这个问题。但只有第三种还能处理 <em>"我的订单显示已送达，可是箱子是空的，我周五之前必须拿到"</em> —— 这是一个没人事先枚举过的请求，需要三次查询加一次政策判断，流水线表达不了，提示链也没有对应的分支。`) +
        note(
          "key",
          "你做的这笔交易",
          p(`能动性为你买来的，是对那些你从没预料过的输入的覆盖能力；它的代价，是你再也无法预测任何一次具体的运行。这门课剩下的全部内容，都是在不放弃覆盖能力的前提下，把这份可预测性一点点买回来的技术。`)
        ),
    },
    {
      id: "core-idea",
      kicker: "核心思想",
      title: "智能体是一个内部带决策的循环",
      html:
        p(`把其他东西全剥掉，这门课要建立的定义就是下面这个。四行代码，而第四行才是关键。`) +
        code({
          title: "全部的想法",
          src: `async function run(goal: string, tools: Tool[]): Promise<string> {
  const messages: Message[] = [{ role: "user", content: goal }];

  while (true) {
    const reply = await model(messages, tools);   // 模型做决定
    messages.push(reply);

    if (reply.stop) return reply.text;            // 它决定停下

    for (const call of reply.toolCalls) {         // 它决定行动
      const result = await runTool(tools, call);  // 世界给出回应
      messages.push({ role: "tool", id: call.id, content: result });
    }
  }
}`,
        }) +
        p(`把这个循环当成一句话来读：<em>模型看一眼到目前为止发生的所有事情，在两个动作里选一个——回答，或者使用工具——如果它用了工具，那个工具的结果就会成为它下一次要看的内容的一部分。</em> 这就是全部的机制。智能体就是一个 <strong>分支条件由模型输出决定的 while 循环</strong>。`) +
        p(`从这四行代码里会掉出三个性质。这门课后面几乎所有的困难，都是它们三个共同造成的。`) +
        `<h3>控制流本身是数据</h3>` +
        p(`在普通软件里，控制流是你写的，编译的时候就定死了。这里，控制流由一个随机函数在运行时产生。你没法读一遍程序就知道它的执行路径，因为这条路径是一个输出，而不是一个源文件。测试、调试、成本估算和安全，全都得围绕这一个事实重新设计。`) +
        `<h3>这个循环没有天然的终点</h3>` +
        p(`上面的代码里没有任何东西能保证 <code>reply.stop</code> 总有一天会变成 true。一个不断决定"我再多查一样东西"的模型，会产生一个每转一圈都在真金白银烧钱的死循环。所有上了生产的智能体都从外部强制终止——靠轮次预算、token 预算，或者一个墙钟时限——因为循环内部靠不住，它给不出终止条件。${ch("c12", "C12")} 完整地讲这个问题。`) +
        `<h3>所有东西都会重新进入上下文</h3>` +
        p(`每个工具结果都被追加进 <code>messages</code>，而 <code>messages</code> 在下一轮会被原封不动地整个重发一遍。一个十步的任务，会把第一步的输出发十次。于是成本随步数呈二次增长，而一个任务能跑多久，最终卡住它的是上下文窗口，而不是模型的智力。${ch("c05", "C05")} 整章都在讲这件事。`) +
        note(
          "",
          "一个有用的反向定义",
          p(`如果你能在运行<em>之前</em>把执行过程画成流程图，那它是一个工作流。如果这张流程图只能在<em>事后</em>照着 trace 画出来，那它是一个智能体。这是 Anthropic 那篇《Building Effective Agents》用的判据，也是最值得记住的一个。`)
        ),
    },
    {
      id: "mechanics",
      kicker: "机制",
      title: "能动性旋钮",
      html:
        p(`"智能体"不是一个非黑即白的标签，它是一个旋钮。对你要做的任何系统，该问的从来不是<em>这算不算智能体</em>，而是<em>这个旋钮应该往右拧到哪一格</em>——每往右一格，能力加一分，可预测性减一分。`) +
        fig({
          label: "图解",
          title: "一个旋钮上的五个位置",
          body: DIAL_SVG,
          caption:
            `第 3 格就是大多数人说"智能体"时指的东西，也是这门课要做的东西。注意那个星号：无界是这个循环的性质，不是你系统的性质——你从外面给它设界，${ch("c12", "C12")} 讲怎么设。`,
        }) +
        `<h3>每拧一格，究竟变了什么</h3>` +
        table(
          ["", "谁选下一步", "事先知道几步", "模型调用次数", "失败方式"],
          [
            ["<b>0 · 流水线</b>", "你", "全部", "0–1", "在某一行崩溃，而你能指出是哪一行"],
            ["<b>1 · 路由</b>", "模型从 N 个里选 1 个", "全部 N 个", "1–2", "选错分支，但执行本身没错"],
            ["<b>2 · 提示链</b>", "你定，模型填空", "那张 DAG", "2–6", "沿着固定路径不断放大的误差"],
            ["<b>3 · 智能体</b>", "模型，每一步都是", "没有", "2–40", "绕圈、跑偏，或者信心十足地做错事"],
            ["<b>4 · 开放式</b>", "模型，还包括造新工具", "没有", "无界", "上面所有的，外加一些全新的"],
          ]
        ) +
        p(`经济上最重要的是中间那一行。相当数量以"智能体"名义发布的系统，其实是披了件外衣的第 1 格或第 2 格，而且它们这样<em>更好</em>：更便宜、更快、可测试，还能跟那个需要签字的人解释清楚。这门课真正在教的本事，是判断一个问题需要哪一格，然后把那一格做好。`) +
        note(
          "warn",
          "默认并不是第 3 格",
          p(`当有效步骤序列的空间大到没法枚举、当下一步真的取决于上一步返回了什么、当你能容忍路径上的波动时，才该伸手去拿能动性。这三条里只要有一条不成立，工作流就会在用户能感知到的每一项指标上碾压你的智能体。${ch("c11", "C11")} 把这件事从品味变成一套设计流程。`)
        ),
    },
    { ...explore, kicker: "动手试试", title: "拧动旋钮，盯着账单" },
    {
      id: "build",
      kicker: "写出来",
      title: "最小的诚实智能体",
      html:
        p(`这一章的可运行文件针对同一个确定性的模拟"模型"实现了全部五个位置，所以你可以在本地跑这个对比，不需要 API key，也不花钱。智能体那一格就是上面那四行循环，外加每个真实系统都需要的两道防线。`) +
        code({
          title: "code/c00_agency_dial.ts（节选）",
          src: `type Decision =
  | { kind: "answer"; text: string }
  | { kind: "call"; tool: string; args: Record<string, unknown> };

export async function agent(goal: string, env: Env, limits: Limits): Promise<Result> {
  const messages: Msg[] = [{ role: "user", content: goal }];
  let calls = 0;
  const deadline = Date.now() + limits.wallClockMs;

  while (true) {
    // 防线 1：循环自己给不出终止条件，所以我们来给。
    if (calls >= limits.maxSteps) return { ok: false, why: "step budget exhausted", messages };
    if (Date.now() > deadline) return { ok: false, why: "deadline exceeded", messages };

    const d: Decision = await env.model(messages);
    calls++;

    if (d.kind === "answer") return { ok: true, text: d.text, messages, calls };

    // 防线 2：模型只能点名一个工具，它没有权力凭空造一个出来。
    const tool = env.tools[d.tool];
    const observation = tool
      ? await tool(d.args).catch((e) => \`ERROR: \${(e as Error).message}\`)
      : \`ERROR: no tool named \${d.tool}. Available: \${Object.keys(env.tools).join(", ")}\`;

    messages.push({ role: "assistant", content: JSON.stringify(d) });
    messages.push({ role: "tool", content: String(observation) });
  }
}`,
        }) +
        p(`这里面有两行，比这个文件其余部分加起来还值钱。`) +
        ul([
          `<strong>预算检查放在最前面，而且在循环里面。</strong> 不是在模型调用之后，也不是包在外面。是最前面，每一轮都做。一个已经把预算烧光的智能体，应该在花掉这笔钱<em>之前</em>停下来，而循环的顶部是唯一能做到这一点的地方。`,
          `<strong>一个不存在的工具名是一条观察，不是一个异常。</strong> 模型幻觉出了 <code>lookup_ordre</code>。抛异常会直接杀死这次运行。把这个拼写错误当成工具结果喂回去，模型下一轮就能自己改过来。这一个决定对端到端成功率的影响，比大多数提示词工程都大。${ch("c03", "C03")} 会把它推广开。`,
        ]) +
        code({
          title: "跑一下",
          lang: "bash",
          plain: true,
          src: `node --experimental-strip-types code/c00_agency_dial.ts

#   C00 · The agency dial — same task, five positions
#
#   live loop:  answered in 4 calls
#               recovered from an unknown-tool typo in one step — the observation carried the fix
#               "Delivered 2024-01-28; the 14-day electronics window has passed."
#
#   difficulty 25 · routine request
#   position        solved  med calls   p99  med tokens  med latency  blast radius
#   0 · pipeline       14%          0     0           0         0.0s  none
#   1 · router         31%          1     1         700         0.5s  read-only
#   2 · chain          53%          3     5       2,600         1.8s  read-only
#   3 · agent          82%          4    21       9,000         7.2s  whatever the tools can do
#   4 · open-ended     85%          8    47      36,000        31.5s  writes its own tools
#
#   difficulty 65 · request nobody planned for
#   position        solved  med calls   p99  med tokens  med latency  blast radius
#   0 · pipeline        0%          0     0           0         0.0s  none
#   1 · router         11%          1     1         700         0.5s  read-only
#   2 · chain          41%          3     5       2,600         1.8s  read-only
#   3 · agent          67%          5    22      11,250         9.0s  whatever the tools can do
#   4 · open-ended     73%          9    48      42,000        36.8s  writes its own tools
#
#   Two things to read off these tables:
#     · positions 0–1 collapse as difficulty rises — a pipeline cannot invent a step
#     · at position 3 the median is fine and the p99 is 4× it. capacity, timeouts and
#       cost are all sized by that tail, and most of this course is about taming it.`,
        }) +
        note(
          "good",
          "永远不需要 key",
          p(`这门课里每一个可运行文件都自带一个确定性的模拟模型，完全可以离线运行。如果某一章接上真实模型会更有意思，同一个文件接受 <code>--live</code> 参数，并读取 <code>ANTHROPIC_API_KEY</code> 或 <code>OPENAI_API_KEY</code>。见<a href="/zh/setup/">本地环境</a>。`)
        ),
    },
    {
      id: "production",
      kicker: "生产实践",
      title: "真实系统里这条线画在哪儿",
      html:
        p(`每个正经框架都在这个旋钮上占了一个位置。把它们当成位置而不是互相竞争的品牌来读，整个版图会比看上去小得多。`) +
        ul([
          `<strong>LangGraph</strong> 从构造上就是第 2 格：你声明一张由节点和边组成的 <code>StateGraph</code>，模型负责填节点。能动性通过按模型输出路由的条件边进来，所以你可以搭出第 3 格——但那张图仍然是你的，而这正是它的用意。`,
          `<strong>AutoGen</strong>（<code>autogen-core</code>）更底层一层：智能体是通过一个运行时交换消息的 actor。能动性取决于这些智能体的处理函数做了什么。${ch("c18", "C18")} 会把那个运行时重新造一遍。`,
          `<strong>OpenAI Agents SDK 和 Claude Agent SDK</strong> 把第 3 格直接做成了产品：一个循环、一份工具清单、一条停止规则，再加上 handoff 和护栏。`,
          `<strong>Claude Code、Codex 和 Cursor 的 agent 模式</strong> 是第 3 格，只是工具面大得不寻常（你的文件系统和你的 shell），因而也配了一套相当严肃的权限层——这正好就是 ${ch("c16", "C16")}。`,
        ]) +
        note(
          "",
          "业界文章里最该记住的那句话",
          p(`Anthropic 的《Building Effective Agents》说得很直白：最成功的落地用的都是能解决问题的最简单模式，而智能体适用于那些多出来的延迟和成本确实换来了任务表现真实提升的场景。后面这二十四章没有任何一句跟这个矛盾。它们讲的是：<em>在你已经确认自己确实需要第 3 格之后</em>，怎么把它做对。`)
        ),
    },
  ],

  exercises: [
    {
      difficulty: "warm-up",
      prompt: `把下面每一个归到 0–4 的某一格，并说出判断依据：(a) 垃圾邮件过滤器，(b) "总结这份 PDF"，(c) 一个先查订单再回答的客服机器人，(d) Claude Code 在修一个失败的测试，(e) 一个读取故障报告、写一段 Python 去查指标、运行它、然后不断迭代的系统。`,
      answer:
        ul([
          `<b>(a) 第 0 格。</b> 分类器哪怕是神经网络，也没有循环，没有工具。一个输入，一个输出，形状固定。`,
          `<b>(b) 第 0 格或第 1 格。</b> 一次模型调用。如果你在调用之前按文档类型分了支，那就是第 1 格。`,
          `<b>(c) 如果是你的代码先查再调模型，第 2 格；如果是模型决定<em>要不要</em>查，第 3 格。</b> 判断依据是那个 <code>if</code> 归谁所有。这是最容易被归错的一种情况。`,
          `<b>(d) 第 3 格。</b> 模型自己选读取/编辑/运行/再读取，顺序取决于测试打印了什么。执行轨迹只能事后画出来。`,
          `<b>(e) 第 4 格。</b> 它在运行时创造了一项新能力。注意"写个脚本然后运行它"正是那个让沙箱变成必选项的动作（${ch("c13", "C13")}）。`,
        ]),
    },
    {
      difficulty: "core",
      prompt: `拿本章那四行循环，列出它所有可能无法终止的方式。对每一种，说出阻止它的那道防线，以及这道防线该放在哪里——循环里面、循环外面，还是提示词里。`,
      answer:
        table(
          ["失败方式", "防线", "放在哪"],
          [
            ["模型永远不发出 <code>stop</code>", "最大步数", "循环里面，最顶端"],
            ["模型在两个工具之间来回横跳", "对 (tool, args) 哈希做循环/重复检测", "循环里面，决策之后"],
            ["某个工具卡住了", "用 <code>AbortSignal</code> 给每个工具设超时", "工具调用外面"],
            ["每一步都成功，但任务本身无界（\"一直监控\"）", "墙钟时限", "循环里面，最顶端"],
            ["上下文一直涨到请求被拒绝", "token 预算 + 压缩", "循环里面，模型调用之前（C05）"],
            ["模型认为自己做完了，但活没干完", "验证步骤；这不是终止 bug，只是长得像", "循环之后（C10）"],
          ]
        ) +
        p(`一般性原则：<em>终止是一个外部性质</em>。写"做完就停"这样的提示词在边际上有点用，但它永远不是一道防线，因为你要防的那个失败恰恰是：模型真心实意地认为自己还没做完。`),
    },
    {
      difficulty: "core",
      prompt: `在模拟器里把位置设成 3，难度设成 20。p99 的调用次数大约是中位数的四倍。解释为什么决定你基础设施账单和超时设置的是这个比值，而不是中位数；以及你会在生产环境里测什么来看到它。`,
      answer:
        p(`容量是按尾部而不是按中间来定的。如果中位数是 4 次调用而 p99 是 17 次，那么在任何一个时刻，都有相当一部分正在飞的运行是长尾运行；它们同时占着连接、占着并发槽位、吃着上下文窗口预算。按中位数设的超时会杀掉十分之一的正常流量；按尾部设的超时则会让一次卡住的运行在有人发现之前烧掉十七次调用的钱。`) +
        p(`该测什么：每次运行的直方图（不是平均值），包括模型调用次数、总 token、墙钟时间和工具调用次数，并且按终止状态打标签——已回答、预算耗尽、出错、被用户取消。智能体运维里最有用的一张图是<em>按终止状态拆分的、分桶的完成步数分布</em>：如果"预算耗尽"那条序列的右尾开始抬头，这是提示词或工具的某次改动让智能体开始乱逛的最早信号。${ch("c20", "C20")} 会把它做出来。`),
    },
    {
      difficulty: "stretch",
      prompt: `写出你在设计评审上真会拿来用的那套决策流程，用来挑旋钮的位置。它要能让产品经理回答得上来，五分钟之内跑完，并且在不确定时默认倒向更低的格位。`,
      answer:
        ol([
          `<b>你能把有效的步骤序列枚举出来吗？</b> 如果能，而且不到十几种，就做第 0–2 格。到此为止；大多数系统就该停在这儿。`,
          `<b>第 N+1 步是不是真的取决于第 N 步返回的<em>内容</em></b>，而不只是取决于它成没成功？"先搜索，再总结"不算；"先搜索，如果结果和政策冲突就改去搜政策库"才算。如果不是，第 2 格。`,
          `<b>路径上的波动，那个对这块业务负责的人能接受吗？</b> 去问受监管的场景、审计的场景，还有"它当时为什么那么干"的场景。如果不能接受，第 2 格及以下，并且只在固定节点内部加模型调用。`,
          `<b>最坏的那一次工具调用，影响半径有多大？</b> 把它写成一句带动词和宾语的话（"给客户发一封邮件"、"删掉一行数据"）。如果这句话让屋里任何一个人感到害怕，那你需要的是第 3 格<em>外加</em> ${ch("c16", "C16")} 的审批，而审批设计从现在起就是工期的一部分，不是后续再说的事。`,
          `<b>你付得起 p99 吗，而不是中位数？</b> 把预期的中位成本乘以四，中位延迟也乘以四。如果这个数字不可接受，那不管你愿不愿意，你要做的都是第 2 格。`,
        ]) +
        p(`不确定时倒向更低格位，之所以重要，是因为升级的路很便宜而降级的路很贵：把一条能用的提示链改成智能体是一天的活；而给一个已经上线的智能体事后补上确定性，意味着你得和所有已经依赖上它那份灵活性的人重新谈一遍。`),
    },
  ],

  qa: [
    {
      q: "一次带工具调用的模型调用算智能体吗？",
      a: p(`一次调用、一个工具结果、一个最终答案，这是退化情形：一个跑两轮的循环。真正起区分作用的性质不是有没有用工具，而是<em>轮数</em>是不是在运行时才决定的。一个被硬编码成恰好只能调一次工具的系统，是加了几步的路由，评估它的时候也该按路由来评。`),
    },
    {
      q: "LangGraph、AutoGen 和各家 SDK 都在那儿了，为什么还要从零写？",
      a:
        p(`因为在生产里真正会坏掉的那些部分，恰好不是框架帮你抽象掉的部分。框架给你的是循环——那只有四行——留给你的是上下文预算、重试语义、工具错误面、评测集和权限模型，而这些才是难的地方。上完这门课你应该去用框架；只不过到那时你能看出它在做什么，以及它没替你做什么。`) +
        p(`举个具体的例子：每个框架都有一个"最大迭代次数"设置。几乎没有哪个框架对"当这个上限触发时，你的智能体该拿它已经做完的那部分活怎么办"有任何主张。那是你的设计问题，而 ${ch("c12", "C12")} 讲的就是它。`),
    },
    {
      q: "模型变强之后，这个旋钮是不是就不重要了？",
      a: p(`它会把各格的天花板往上抬，也会改变某个具体问题该落在哪一格，但结构本身不变。更强的模型会抬高第 3 格的成功率上限、缩短它的长尾；它不会让任何一次具体运行变得可预测，不会缩小 <code>email_customer</code> 的影响半径，也不会凭空给这个循环一个终止条件。能力和控制是两根独立的轴，而这门课讲的是第二根。`),
    },
    {
      q: "'多智能体系统'在这个旋钮上处在什么位置？",
      a: p(`在旁边，而不是更靠右。多个智能体是一个<em>拓扑</em>选择，主要用来隔离上下文和并行处理互相独立的工作；一个由主管按固定顺序调用固定专家的五智能体系统，就是零件更多的第 2 格。${ch("c17", "C17")} 把拓扑当成一根独立的轴来处理，并且对"这有多经常是个错误的第一选择"说话相当不客气。`),
    },
    {
      q: "课程说'不用框架'。那它到底用不用任何库？",
      a: p(`只用 Node 的标准库和 <code>fetch</code>。没有 LangChain，没有 SDK，没有向量数据库，也没有 <code>zod</code>——你会在 ${ch("c02", "C02")} 写一个 60 行的 schema 校验器，在 ${ch("c06", "C06")} 写一个向量索引，因为亲手写一遍，是你搞明白那些真家伙替你做了哪些取舍的方式。这些代码是拿来读的，不是拿来当依赖的。`),
    },
  ],

  project: {
    title: "项目 · 位置审计",
    brief:
      p(`挑一个已经存在的 LLM 功能——你自己做过的、公司里的，或者一个你用过、能精确描述出来的（客服机器人、IDE 助手、摘要工具）。拿旋钮审计它一遍，写成一页纸。`) +
      p(`这个项目不写代码，而它是整门课里最值的二十分钟。后面每一章你都会觉得更好懂，只要你脑子里有一个已经分过类的具体系统。`),
    spec: [
      "它在旋钮的哪一格，以及把它钉在那一格的具体证据——指出是哪一行代码或哪个可观察到的行为，不要拿宣传语当证据。",
      "完整的工具面，每一项写成带动词和宾语的句子。每一项标上<em>读</em>或<em>写</em>。",
      "最坏的那一次工具调用的影响半径，用一句话写清楚。",
      "今天是什么让这个循环停下来的，以及以那种方式停下来时，做到一半的活会怎么样。",
      "用一句话说明这个位置对不对，以及你会把它挪到哪一格。",
    ],
    stretch: [
      "从日志里估出每个请求的模型调用次数中位数和 p99；如果没有日志，就故意构造十个困难请求来估。",
      "写下最可能让它失败的三个输入，并预测每一个的失败方式。把这个文件留着——你会在 C19 把它们当评测集重跑一遍。",
    ],
  },

  quiz: [
    {
      q: "哪一个性质最能把智能体和提示链区分开？",
      options: [
        "智能体的控制流是由模型输出在运行时产生的，所以执行路径在运行之前画不出来",
        "智能体会用工具，提示链不会",
        "智能体用的模型更大",
        "智能体会跨轮次维护对话历史",
      ],
      answer: 0,
      why:
        "提示链一样会用工具、一样会保留历史。真正起区分作用的性质是谁来决定下一步：在提示链里那张图是你画的，在智能体里那张图是一个输出。一个好用的判据是：你能在运行前拿出流程图，还是只能事后照着 trace 画。",
    },
    {
      q: "为什么智能体的成本增长快于步数的线性增长？",
      options: [
        "因为每一步都要把累积的整个消息历史重发一遍，于是第 N 步要为第 1..N-1 步再付一次钱",
        "因为模型对一个会话里靠后的调用收费更高",
        "因为工具调用按溢价单独计费",
        "因为每一步都需要比上一步更大的模型",
      ],
      answer: 0,
      why:
        "循环会把每条观察追加进 `messages`，然后把整个数组重发一遍。十步意味着第一个工具结果被传输了十次。这种近似二次的增长，正是上下文工程（C05）是一门成本学科而不只是质量学科的原因。",
    },
    {
      q: "模型要求调用 `lookup_ordre`——一个并不存在的工具。最好的处理方式是什么？",
      options: [
        "把一个列出了可用工具名的错误字符串当作工具结果返回，然后继续循环",
        "抛异常，用一段清晰的调用栈结束这次运行",
        "模糊匹配到 `lookup_order` 然后调用它",
        "用更高的温度重试同一次模型调用",
      ],
      answer: 0,
      why:
        "把失败当成一条观察喂回去，模型下一轮就能自我纠正，而这正是循环的用途。抛异常会丢掉一次本来还有救的运行。悄悄做模糊匹配比前两者都糟：它掩盖了'你的工具名容易混淆'这个真实信号，而且早晚有一天会匹配到错误的工具。",
    },
    {
      q: "步数预算检查应该放在循环的什么位置？",
      options: [
        "循环体的最顶端，在模型调用之前",
        "模型调用之后，这样最后一次决策还能被记录下来",
        "包在整次运行外面，结束时检查一次",
        "写在系统提示词里，作为给模型的一条指令",
      ],
      answer: 0,
      why:
        "在模型调用之前检查，才是真正省下那笔钱的做法。放在之后检查，等于为你本想避免的那次调用付了钱；放在最后检查，那根本不算一道防线。提示词里那条指令值得加，但它永远不是防线，因为你要防的情形恰恰是：模型真心认为自己还没做完。",
    },
    {
      q: "为一个第 3 格的智能体设置超时和并发时，哪个数字最重要？",
      options: [
        "完成步数的 p99",
        "平均步数",
        "单次模型调用的中位延迟",
        "注册的工具总数",
      ],
      answer: 0,
      why:
        "长尾运行会同时占住并发槽位、连接和上下文预算，所以容量由尾部决定。按中位数推出来的超时会杀掉正常的长运行；按尾部推出来的超时会放任卡死的运行烧预算。正确的工具是按终止状态拆分的直方图，而不是平均值。",
    },
    {
      q: "一个团队提议把一条能正常工作的两次调用提示链换成智能体，理由是'处理边缘情况'。最有力的反驳是什么？",
      options: [
        "如果有效步骤序列可以枚举，而且下一步并不取决于上一步的内容，那么能动性只会增加成本、延迟和波动，什么也买不到",
        "在所有任务上智能体都不如提示链准确",
        "智能体没法用提示链能用的那些工具",
        "智能体需要一个微调过的模型",
      ],
      answer: 0,
      why:
        "只有当有效路径的空间大到无法枚举、并且下一步确实取决于上一步返回了什么时，能动性才值它的价钱。正确的回应是那套设计流程：列出步骤序列，找一找有没有真实的内容依赖，然后再去算 p99 的账。",
    },
  ],

  continues:
    p(`上面那个循环把 <code>model(messages, tools)</code> 当成一个原语来调用。它不是。在任何东西开始做任何决定之前，你需要一个带类型、支持流式、会重试、把成本记清楚的方法，把一个消息数组变成一个决策——而这一个函数的形状，会约束后面每一个设计选择。${ch("c01", "C01")} 会把它造出来。`),
};

export default chapter;
